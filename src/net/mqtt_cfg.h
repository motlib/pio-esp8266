#ifndef MQTT_CFG_H
#define MQTT_CFG_H

/** After this time (task cycles) we give up waiting for connection to the
 * broker and try with a new connection attempt. */
#define MQTT_CONNECT_TIMEOUT 1000


#endif /* MQTT_CFG_H */
